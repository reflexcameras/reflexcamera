source "https://rubygems.org"

# Keep local builds aligned with the versions used by GitHub Pages.
gem "github-pages", "~> 232", group: :jekyll_plugins

# Faraday 2 ships retry middleware as a separate gem.
gem "faraday-retry", "~> 2.0"
